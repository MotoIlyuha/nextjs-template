---
description: "Использовать Zustand для UI-состояния, React Query для серверного состояния"
globs:
  - "app/**/*"
  - "components/**/*"
  - "hooks/**/*"
  - "stores/**/*"
alwaysApply: true
---

> 🎯 **Zustand — только для клиентского (UI) состояния:**
> - Открытие/закрытие модалок
> - Активный таб навигации
> - Локальные формы (черновики)
> - Состояние кнопок, загрузки (если не связано с запросом)
> - Тема Telegram (`themeParams`)
> 
> 🎯 **React Query (TanStack Query v5) — только для серверного состояния:**
> - Данные из Supabase (ученики, занятия, задания, файлы, финансы)
> - Загрузка, ошибки, кэш, инвалидация, мутации
> - Пагинация, ленивая загрузка, фоновые обновления
> 
> 📌 Пример Zustand:
> ```ts
> // stores/uiStore.ts
> const useUIStore = create((set) => ({
>   isStudentFormOpen: false,
>   openStudentForm: () => set({ isStudentFormOpen: true }),
> }));
> ```
> 
> 📌 Пример React Query:
> ```ts
> // hooks/useStudents.ts
> const { data: students, isLoading } = useQuery({
>   queryKey: ['students', teacherId],
>   queryFn: fetchStudents,
> });
> ```
> 
> 🚫 Не используй Zustand для хранения данных из API.
> 🚫 Не используй React Query для управления UI-состоянием (модалки, табы).
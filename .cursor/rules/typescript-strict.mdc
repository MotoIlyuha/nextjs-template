---
description: "TypeScript strict mode — все типы обязательны"
globs:
  - "**/*.ts"
  - "**/*.tsx"
alwaysApply: true
---

> ✅ **Все компоненты, хуки, функции, API-роуты — должны быть строго типизированы.**
> - Используй `interface` или `type` для всех пропсов, состояний, ответов API.
> - Не используй `any`, `unknown` без крайней необходимости.
> - Генерируй типы Supabase через `supabase gen types typescript`.
> 
> 📌 Пример:
> ```ts
> interface Student {
>   id: string;
>   first_name: string;
>   price_per_lesson: number;
> }
> 
> const useStudents = (teacherId: string): UseQueryResult<Student[]> => { ... }
> ```
> 
> 🚫 Запрещено:
> ```ts
> const data: any = await fetch(...);
> function handleSubmit(data) { ... } // без типов
> ```
> 
> ✅ Используй Zod для валидации форм → генерируй типы из схем.